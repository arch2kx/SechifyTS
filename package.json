{
  "name": "yet-another-sechify-fork",
  "version": "1.1.3",
  "description": "A Chrome/Firefox extension that makes every thumbnail on YouTube have art from the artist/animator BlueSechi.",
  "main": "main.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:manifest": "node scripts/build-manifest.js",
    "build": "npm run build:manifest && tsc",
    "zip": "cd dist && zip -r ../SechifyTS.zip . -x \".*\" && cd ..",
    "package": "npm run build && npm run zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "@types/firefox-webext-browser": "^143.0.0",
    "esbuild": "^0.28.1",
    "strip-json-comments": "^5.0.3",
    "typescript": "^6.0.3"
  }
}
